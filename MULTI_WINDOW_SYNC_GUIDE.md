# ISLES Browser 多窗口同步

多窗口同步可以将一个已运行配置文件窗口中的选定输入事件同步到其他已运行的配置文件窗口。它适用于重复导航和受控测试等场景，前提是各窗口具有相同的页面布局和状态。

## 开始前

1. 创建或选择需要使用的配置文件窗口。
2. 打开主窗口和全部目标窗口。
3. 选择一个运行中的窗口作为主窗口，选择一个或多个运行中的窗口作为目标窗口。
4. 开始同步前，确认所有窗口打开了布局一致的页面。

同步使用相对指针坐标。不同的页面缩放比例、窗口尺寸、扩展弹窗和页面状态都可能导致目标窗口出现不同结果。

## 可用控制项

同步服务支持以下选项：

| 选项 | 默认值 | 作用 |
| --- | --- | --- |
| 鼠标输入 | 启用 | 同步指针移动和点击。 |
| 键盘输入 | 启用 | 同步按键和文本输入。 |
| 滚轮输入 | 启用 | 同步页面滚动。 |
| CDP 滚动同步 | 禁用 | 通过本地调试连接定期对齐页面滚动位置。 |
| 鼠标节流 | 10 ms | 限制高频指针事件的发送速率。 |
| 鼠标移动阈值 | 2 px | 忽略极小的指针位移。 |
| 滚轮节流 | 50 ms | 限制滚轮事件的发送速率。 |

关闭或替换参与同步的浏览器窗口前，请先停止同步。

## 系统权限

在 macOS 上，ISLES Browser 需要“辅助功能”权限才能捕获和发送输入事件。请在“系统设置 > 隐私与安全性 > 辅助功能”中授予权限；如果权限没有立即生效，请重启应用。

在 Windows 上，如果目标窗口以更高权限运行，可能需要提升 ISLES Browser 的权限。请确保应用和目标浏览器窗口使用相同的权限级别。

## 使用建议

- 先用两个窗口确认效果，再逐步增加目标窗口。
- 保持浏览器缩放比例、窗口尺寸和页面状态一致。
- 只保留实际需要的目标窗口数量。
- 不需要键盘或滚轮同步时，将对应选项关闭。
- 只有所有配置文件均可使用本地调试连接时，才启用 CDP 滚动同步。

## 常见问题

**无法启动同步**

请确认选择的主窗口和目标窗口均已打开，每个窗口都必须有对应的浏览器进程。

**事件被捕获但目标窗口没有响应**

请检查 macOS 的辅助功能权限或 Windows 的进程权限，并确认鼠标位于主浏览器窗口内。

**目标页面逐渐不一致**

请确认所有窗口的缩放比例和页面布局一致。可以减少目标窗口数量，或在合适时启用 CDP 滚动同步。

**滚动响应延迟**

可以增大滚轮节流时间以降低负载，或者在不需要时关闭滚轮同步。
